import React, { Component } from "react";

class HabitReset extends Component {
  render() {
    return (
      <footer>
        <button className="habit-button habit-reset">Reset All</button>
      </footer>
    );
  }
}

export default HabitReset;
